# PostgreSQL canonical, Elasticsearch projection

Status: ACCEPTED

PostgreSQL is canonical R1 state. Elasticsearch is the first search/retrieval projection after capability audit. No uncontrolled dual writes; use transactional outbox.
