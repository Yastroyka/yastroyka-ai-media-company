name: Owner Ready Bridge

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull request number
        required: true
        type: string
      expected_head_sha:
        description: Exact expected PR head SHA
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write

jobs:
  validate-ready-transition:
    name: Validate Ready transition
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed placeholder
        run: |
          echo "TASK-025 bridge contract installed"
          echo "Implementation remains behind validated owner gate"
